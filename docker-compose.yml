services:
  backend:
    build: .
    ports:
      - "8084:8084"
    env_file:
      - .env
    environment:
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:3004}
    restart: unless-stopped
